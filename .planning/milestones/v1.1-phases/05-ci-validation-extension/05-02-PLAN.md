---
phase: 05-ci-validation-extension
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .github/workflows/test.yml
autonomous: true
requirements: [CI-05]
must_haves:
  truths:
    - "test.yml helm-lint job contains six new TLS lint+template steps"
    - "test.yml helm-lint job runs helm-template-test.sh as its final step"
    - "All seven new steps reference files created by Plan 01"
    - "No --validate flag appears in any helm template step"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "Extended helm-lint job with TLS mode validation"
      contains: "Lint (TLS"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "chart/clay/ci/tls-letsencrypt-values.yaml"
      via: "--values argument in lint and template steps"
      pattern: "--values chart/clay/ci/tls-letsencrypt-values.yaml"
    - from: ".github/workflows/test.yml"
      to: "chart/clay/ci/tls-selfsigned-values.yaml"
      via: "--values argument in lint and template steps"
      pattern: "--values chart/clay/ci/tls-selfsigned-values.yaml"
    - from: ".github/workflows/test.yml"
      to: "chart/clay/ci/tls-custom-values.yaml"
      via: "--values argument in lint and template steps"
      pattern: "--values chart/clay/ci/tls-custom-values.yaml"
    - from: ".github/workflows/test.yml"
      to: "chart/tests/helm-template-test.sh"
      via: "run: invocation as final step"
      pattern: "chart/tests/helm-template-test.sh"
---

<objective>
Extend the test.yml helm-lint job with six TLS lint+template steps and a behavioral test invocation step, so CI validates all three TLS modes on every push.

Purpose: CI-05 requires that the Helm validation job covers letsencrypt, selfsigned, and custom TLS modes. The behavioral test script (23 assertions covering INGR-01..04, TLS-01..03) must also run in CI on every push.

Output: Seven new steps appended to the helm-lint job in .github/workflows/test.yml.
</objective>

<execution_context>
@/shared/pottery-shop/.claude/get-shit-done/workflows/execute-plan.md
@/shared/pottery-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-validation-extension/05-CONTEXT.md
@.planning/phases/05-ci-validation-extension/05-RESEARCH.md
@.planning/phases/05-ci-validation-extension/05-01-SUMMARY.md

<interfaces>
<!-- Current test.yml structure. New steps go after the last existing step (Template (external mode)). -->

From .github/workflows/test.yml (existing helm-lint job):
```yaml
  helm-lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Lint (managed mode)
        run: helm lint chart/clay/ --values chart/clay/ci/managed-values.yaml

      - name: Lint (external mode)
        run: helm lint chart/clay/ --values chart/clay/ci/external-values.yaml

      - name: Template (managed mode)
        run: helm template clay chart/clay/ --values chart/clay/ci/managed-values.yaml

      - name: Template (external mode)
        run: helm template clay chart/clay/ --values chart/clay/ci/external-values.yaml
```

Step naming pattern to follow (per D-08):
- `Lint (TLS — letsencrypt mode)`
- `Template (TLS — letsencrypt mode)`
- `Lint (TLS — selfsigned mode)`
- etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append seven TLS validation steps to helm-lint job in test.yml</name>
  <files>.github/workflows/test.yml</files>

  <read_first>
    .github/workflows/test.yml
    chart/clay/ci/tls-letsencrypt-values.yaml
    chart/clay/ci/tls-selfsigned-values.yaml
    chart/clay/ci/tls-custom-values.yaml
    chart/tests/helm-template-test.sh
  </read_first>

  <action>
Append exactly seven new steps to the end of the `helm-lint` job in `.github/workflows/test.yml`, after the existing "Template (external mode)" step. Use lint-then-template ordering per mode (the established pattern). Per D-09, no `--validate` flag on any `helm template` command.

The seven steps to append (in this exact order, with these exact names per D-08 and D-10):

```yaml
      - name: Lint (TLS — letsencrypt mode)
        run: helm lint chart/clay/ --values chart/clay/ci/tls-letsencrypt-values.yaml

      - name: Template (TLS — letsencrypt mode)
        run: helm template clay chart/clay/ --values chart/clay/ci/tls-letsencrypt-values.yaml

      - name: Lint (TLS — selfsigned mode)
        run: helm lint chart/clay/ --values chart/clay/ci/tls-selfsigned-values.yaml

      - name: Template (TLS — selfsigned mode)
        run: helm template clay chart/clay/ --values chart/clay/ci/tls-selfsigned-values.yaml

      - name: Lint (TLS — custom mode)
        run: helm lint chart/clay/ --values chart/clay/ci/tls-custom-values.yaml

      - name: Template (TLS — custom mode)
        run: helm template clay chart/clay/ --values chart/clay/ci/tls-custom-values.yaml

      - name: Behavioral tests (INGR-01..04, TLS-01..03)
        run: chart/tests/helm-template-test.sh
```

Do NOT modify any existing steps. Do NOT add `--validate` to any helm template command. Do NOT create a new job — all steps go in the existing `helm-lint` job (per D-07).

Indentation: match the existing 6-space indentation for step entries (the file uses 2-space YAML with 3 levels of nesting for steps under jobs.helm-lint.steps).
  </action>

  <verify>
    <automated>grep -c "name: Lint (TLS" .github/workflows/test.yml | grep -q "3" && grep -c "name: Template (TLS" .github/workflows/test.yml | grep -q "3" && grep -c "helm-template-test.sh" .github/workflows/test.yml | grep -q "1" && grep -c "\-\-validate" .github/workflows/test.yml | grep -q "0" && echo "ALL PASS"</automated>
  </verify>

  <acceptance_criteria>
- `.github/workflows/test.yml` contains `name: Lint (TLS — letsencrypt mode)` (exact string)
- `.github/workflows/test.yml` contains `name: Template (TLS — letsencrypt mode)` (exact string)
- `.github/workflows/test.yml` contains `name: Lint (TLS — selfsigned mode)` (exact string)
- `.github/workflows/test.yml` contains `name: Template (TLS — selfsigned mode)` (exact string)
- `.github/workflows/test.yml` contains `name: Lint (TLS — custom mode)` (exact string)
- `.github/workflows/test.yml` contains `name: Template (TLS — custom mode)` (exact string)
- `.github/workflows/test.yml` contains `name: Behavioral tests (INGR-01..04, TLS-01..03)` (exact string)
- `.github/workflows/test.yml` contains `run: chart/tests/helm-template-test.sh` (exact string)
- `.github/workflows/test.yml` contains `--values chart/clay/ci/tls-letsencrypt-values.yaml` (appears twice: lint + template)
- `.github/workflows/test.yml` contains `--values chart/clay/ci/tls-selfsigned-values.yaml` (appears twice: lint + template)
- `.github/workflows/test.yml` contains `--values chart/clay/ci/tls-custom-values.yaml` (appears twice: lint + template)
- `.github/workflows/test.yml` does NOT contain `--validate` anywhere
- The seven new steps are all inside the `helm-lint` job (not in a new job, not in the `test` job)
- Total step count in helm-lint job is 13 (6 existing + 7 new)
  </acceptance_criteria>

  <done>
The helm-lint job in test.yml contains 13 steps total: 6 existing (checkout, setup-helm, 2 lint, 2 template for managed/external) plus 7 new (3 TLS lint, 3 TLS template, 1 behavioral test). No --validate flag. All step names match D-08. Behavioral test is the final step per D-10.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow -> chart rendering | GitHub Actions runner executes helm lint/template; no cluster access |
| CI workflow -> test script | Runner executes helm-template-test.sh with system bash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | test.yml workflow file modification | accept | Protected by branch protection rules on main; all changes visible in PR diff. No secrets or credentials are introduced by these steps. |
| T-05-04 | Denial of Service | Seven additional CI steps increase job duration | accept | Each helm lint/template step takes ~2 seconds; helm-template-test.sh runs in ~5 seconds. Total additional time: ~20 seconds. Negligible impact on CI duration. |
</threat_model>

<verification>
1. `grep -c "name: Lint (TLS" .github/workflows/test.yml` returns 3
2. `grep -c "name: Template (TLS" .github/workflows/test.yml` returns 3
3. `grep -c "helm-template-test.sh" .github/workflows/test.yml` returns 1
4. `grep -c "\-\-validate" .github/workflows/test.yml` returns 0
5. `chart/tests/helm-template-test.sh` runs locally and all 23 assertions pass
6. The existing four managed/external lint+template steps are unchanged
</verification>

<success_criteria>
CI-05 is fully satisfied: test.yml contains six new lint+template steps (one lint + one template per TLS mode) plus one behavioral test step, all in the helm-lint job, none using --validate. Combined with CI-04 (Plan 01), the CI pipeline validates all three TLS modes on every push.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-validation-extension/05-02-SUMMARY.md`
</output>
