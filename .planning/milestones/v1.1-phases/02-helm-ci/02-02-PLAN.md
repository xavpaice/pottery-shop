---
phase: 02-helm-ci
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/test.yml
  - chart/clay/ci/managed-values.yaml
  - chart/clay/ci/external-values.yaml
autonomous: true
requirements: [CI-01, CI-02, CI-03]

must_haves:
  truths:
    - "CI test job does not install gcc or any C compiler"
    - "CI test job runs go vet, make test, and make build with CGO_ENABLED=0"
    - "CI helm-lint job runs helm dependency update before linting"
    - "CI helm-lint job lints and templates in both managed and external modes"
    - "CI values files exist in chart/clay/ci/ for both modes"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "Updated test job (no gcc) and extended helm-lint job (dependency update, dual-mode lint+template)"
      contains: "helm dependency update"
    - path: "chart/clay/ci/managed-values.yaml"
      provides: "Test values for managed CNPG mode"
      contains: "managed: true"
    - path: "chart/clay/ci/external-values.yaml"
      provides: "Test values for external DSN mode"
      contains: "managed: false"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "chart/clay/ci/managed-values.yaml"
      via: "helm lint --values and helm template --values references"
      pattern: "chart/clay/ci/managed-values.yaml"
    - from: ".github/workflows/test.yml"
      to: "chart/clay/ci/external-values.yaml"
      via: "helm lint --values and helm template --values references"
      pattern: "chart/clay/ci/external-values.yaml"
    - from: ".github/workflows/test.yml"
      to: "chart/clay/Chart.yaml"
      via: "helm dependency update downloads subchart declared in Chart.yaml"
      pattern: "helm dependency update chart/clay/"
---

<objective>
Update the CI pipeline to reflect the CGO-free build, testcontainers-go test execution,
and dual-mode Helm validation. The test job drops gcc installation and the helm-lint job
gains subchart dependency resolution plus helm template validation for both managed and
external modes.

Purpose: CI catches Helm template errors, CGO regressions, and mode-specific rendering
issues on every push before merge.

Output: Updated test.yml workflow, two CI values files in chart/clay/ci/.
</objective>

<execution_context>
@/shared/pottery-shop/.claude/get-shit-done/workflows/execute-plan.md
@/shared/pottery-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-helm-ci/02-CONTEXT.md
@.planning/phases/02-helm-ci/02-RESEARCH.md

<interfaces>
<!-- Current state of .github/workflows/test.yml -->

From .github/workflows/test.yml:
```yaml
name: Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Install dependencies          # <-- REMOVE this step (D-14)
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc

      - name: Verify dependencies
        run: go mod verify

      - name: Run tests
        run: make test

      - name: Build
        run: make build

  helm-lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Lint Helm chart              # <-- REPLACE with multi-step dual-mode validation
        run: make helm-lint
```

From Makefile (relevant targets):
```makefile
build:
	CGO_ENABLED=0 $(GO) build -o $(BINARY) ./cmd/server

test:
	CGO_ENABLED=0 $(GO) test ./...

helm-lint:
	helm lint chart/clay/
```

Note: `make test` and `make build` already set `CGO_ENABLED=0`. The CI step that installed gcc
was a leftover from the SQLite era. Removing the "Install dependencies" step is sufficient.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test.yml test job — remove gcc, add go vet</name>
  <read_first>
    - .github/workflows/test.yml
    - Makefile (confirm CGO_ENABLED=0 is already set in make test and make build)
    - .planning/phases/02-helm-ci/02-CONTEXT.md (D-14, D-15, D-16)
  </read_first>
  <files>.github/workflows/test.yml</files>
  <action>
Modify the `test:` job in `.github/workflows/test.yml`:

1. **Remove the "Install dependencies" step entirely** (per D-14). Delete these lines:
```yaml
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc
```

2. **Add a "Run vet" step** after "Verify dependencies" and before "Run tests" (per CI-02):
```yaml
      - name: Run vet
        run: go vet ./...
```

The updated `test:` job should look like:
```yaml
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Verify dependencies
        run: go mod verify

      - name: Run vet
        run: go vet ./...

      - name: Run tests
        run: make test

      - name: Build
        run: make build
```

Do NOT modify the `helm-lint:` job in this task — that is handled in Task 2.
Per D-15: testcontainers-go uses Docker daemon pre-installed on `ubuntu-latest` — no extra setup step needed. `make test` runs `CGO_ENABLED=0 go test ./...` which includes testcontainers integration tests.
Per D-16: Do NOT touch `.github/workflows/integration-test.yml`.
  </action>
  <verify>
    <automated>
cd /shared/pottery-shop && ! grep -q "apt-get" .github/workflows/test.yml && grep -q "go vet" .github/workflows/test.yml && grep -q "make test" .github/workflows/test.yml && grep -q "make build" .github/workflows/test.yml
    </automated>
  </verify>
  <acceptance_criteria>
- .github/workflows/test.yml does NOT contain string `apt-get`
- .github/workflows/test.yml does NOT contain string `gcc`
- .github/workflows/test.yml contains `go vet ./...` as a step in the test job
- .github/workflows/test.yml still contains `go mod verify` step
- .github/workflows/test.yml still contains `make test` step
- .github/workflows/test.yml still contains `make build` step
- .github/workflows/test.yml still contains `actions/setup-go@v5` with `go-version: '1.26'`
- .github/workflows/integration-test.yml is NOT modified (verify file not in git diff)
  </acceptance_criteria>
  <done>CI test job no longer installs gcc; go vet step added; make test runs testcontainers integration tests via Docker on ubuntu-latest; make build confirms CGO-free compilation</done>
</task>

<task type="auto">
  <name>Task 2: CI values files and helm-lint job — dual-mode lint + template</name>
  <read_first>
    - .github/workflows/test.yml (after Task 1 modifications — re-read to see current state)
    - .planning/phases/02-helm-ci/02-RESEARCH.md (Pattern 5, Pattern 6 for exact YAML)
    - .planning/phases/02-helm-ci/02-CONTEXT.md (D-17, D-18)
  </read_first>
  <files>chart/clay/ci/managed-values.yaml, chart/clay/ci/external-values.yaml, .github/workflows/test.yml</files>
  <action>
**Step 1: Create chart/clay/ci/ directory and values files (per D-18)**

Create `chart/clay/ci/managed-values.yaml`:
```yaml
# CI test values for managed CNPG mode
postgres:
  managed: true
  cluster:
    instances: 1
    storage:
      size: 1Gi
      storageClass: ""
  external:
    dsn: ""

cloudnative-pg:
  enabled: true
```

Create `chart/clay/ci/external-values.yaml`:
```yaml
# CI test values for external Postgres mode
postgres:
  managed: false
  cluster:
    instances: 1
    storage:
      size: 1Gi
      storageClass: ""
  external:
    dsn: "postgresql://user:pass@external-host:5432/clay"

cloudnative-pg:
  enabled: false
```

**Step 2: Update the helm-lint job in .github/workflows/test.yml (per D-17, CI-03)**

Replace the entire `helm-lint:` job with:
```yaml
  helm-lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Add CNPG Helm repo
        run: helm repo add cnpg https://cloudnative-pg.github.io/charts

      - name: Update Helm dependencies
        run: helm dependency update chart/clay/

      - name: Lint (managed mode)
        run: helm lint chart/clay/ --values chart/clay/ci/managed-values.yaml

      - name: Lint (external mode)
        run: helm lint chart/clay/ --values chart/clay/ci/external-values.yaml

      - name: Template (managed mode)
        run: helm template clay chart/clay/ --values chart/clay/ci/managed-values.yaml

      - name: Template (external mode)
        run: helm template clay chart/clay/ --values chart/clay/ci/external-values.yaml
```

Key changes from existing helm-lint job:
- Added "Add CNPG Helm repo" step (required before helm dependency update)
- Added "Update Helm dependencies" step (downloads CNPG subchart; required before lint/template per RESEARCH.md Pitfall 3)
- Replaced single `make helm-lint` with explicit `helm lint` for both modes using ci/ values files
- Added `helm template` validation for both modes (catches template rendering errors beyond lint)

Note: The existing `helm-lint` Makefile target (`helm lint chart/clay/`) still works for local use with default values. The CI job uses explicit commands with per-mode values files for comprehensive validation.
  </action>
  <verify>
    <automated>
cd /shared/pottery-shop && test -f chart/clay/ci/managed-values.yaml && test -f chart/clay/ci/external-values.yaml && grep "managed: true" chart/clay/ci/managed-values.yaml && grep "managed: false" chart/clay/ci/external-values.yaml && grep "helm dependency update" .github/workflows/test.yml && grep "managed-values.yaml" .github/workflows/test.yml && grep "external-values.yaml" .github/workflows/test.yml && grep "helm template" .github/workflows/test.yml && grep "helm repo add" .github/workflows/test.yml
    </automated>
  </verify>
  <acceptance_criteria>
- chart/clay/ci/managed-values.yaml exists and contains `managed: true` and `cloudnative-pg:` with `enabled: true`
- chart/clay/ci/external-values.yaml exists and contains `managed: false` and `dsn: "postgresql://user:pass@external-host:5432/clay"` and `cloudnative-pg:` with `enabled: false`
- .github/workflows/test.yml helm-lint job contains `helm repo add cnpg https://cloudnative-pg.github.io/charts`
- .github/workflows/test.yml helm-lint job contains `helm dependency update chart/clay/`
- .github/workflows/test.yml helm-lint job contains `helm lint chart/clay/ --values chart/clay/ci/managed-values.yaml`
- .github/workflows/test.yml helm-lint job contains `helm lint chart/clay/ --values chart/clay/ci/external-values.yaml`
- .github/workflows/test.yml helm-lint job contains `helm template clay chart/clay/ --values chart/clay/ci/managed-values.yaml`
- .github/workflows/test.yml helm-lint job contains `helm template clay chart/clay/ --values chart/clay/ci/external-values.yaml`
- .github/workflows/test.yml does NOT contain `make helm-lint` (replaced with explicit commands)
- .github/workflows/test.yml still contains `azure/setup-helm@v4`
  </acceptance_criteria>
  <done>CI values files created for both modes; helm-lint job downloads CNPG subchart dependency, lints both modes, and templates both modes; Helm rendering errors are caught before merge</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> CNPG chart repo | CI downloads subchart from external Helm repository |
| CI values files -> helm template | Test values contain dummy credentials (expected) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | CNPG Helm repo in CI | accept | CI downloads CNPG chart from `https://cloudnative-pg.github.io/charts` — HTTPS transport, official CNPG repo. Chart version pinned to 0.28.0 in Chart.yaml. Supply chain attack would require compromise of CNPG GitHub Pages hosting. Accepted at ASVS L1 — standard practice for Helm chart dependencies. |
| T-02-06 | Information Disclosure | external-values.yaml dummy DSN | accept | `chart/clay/ci/external-values.yaml` contains `postgresql://user:pass@external-host:5432/clay` — this is a dummy DSN for CI template validation, not real credentials. The file is committed to the repo intentionally. |
</threat_model>

<verification>
1. `.github/workflows/test.yml` does not contain `apt-get` or `gcc`
2. `.github/workflows/test.yml` test job has `go vet ./...` step
3. `.github/workflows/test.yml` helm-lint job has `helm repo add cnpg` step
4. `.github/workflows/test.yml` helm-lint job has `helm dependency update chart/clay/` step
5. `.github/workflows/test.yml` helm-lint job validates both managed and external modes with lint AND template
6. `chart/clay/ci/managed-values.yaml` sets `postgres.managed: true` and `cloudnative-pg.enabled: true`
7. `chart/clay/ci/external-values.yaml` sets `postgres.managed: false` and `cloudnative-pg.enabled: false`
</verification>

<success_criteria>
- CI test job runs cleanly without gcc: go vet, make test (testcontainers), make build (CGO-free)
- CI helm-lint job resolves CNPG subchart dependency before validation
- CI validates Helm chart rendering in both managed and external modes
- CI values files exist in chart/clay/ci/ following helm chart-testing convention
- integration-test.yml is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/02-helm-ci/02-02-SUMMARY.md`
</output>
