---
phase: 05-ci-validation-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chart/clay/ci/tls-letsencrypt-values.yaml
  - chart/clay/ci/tls-selfsigned-values.yaml
  - chart/clay/ci/tls-custom-values.yaml
autonomous: true
requirements: [CI-04]
must_haves:
  truths:
    - "Three TLS CI values files exist under chart/clay/ci/"
    - "Each file passes helm lint independently"
    - "Each file passes helm template independently (no --validate)"
    - "All three files are self-contained (no layering, each is a single --values argument)"
  artifacts:
    - path: "chart/clay/ci/tls-letsencrypt-values.yaml"
      provides: "CI values for letsencrypt TLS mode"
      contains: "mode: letsencrypt"
    - path: "chart/clay/ci/tls-selfsigned-values.yaml"
      provides: "CI values for selfsigned TLS mode"
      contains: "mode: selfsigned"
    - path: "chart/clay/ci/tls-custom-values.yaml"
      provides: "CI values for custom TLS mode"
      contains: "mode: custom"
  key_links:
    - from: "chart/clay/ci/tls-letsencrypt-values.yaml"
      to: "chart/clay/values.yaml"
      via: "helm --values overlay"
      pattern: "ingress.tls.mode: letsencrypt"
    - from: "chart/clay/ci/tls-custom-values.yaml"
      to: "chart/tests/helm-template-test.sh"
      via: "secretName must match CUSTOM_INGRESS variable"
      pattern: "secretName: my-tls"
---

<objective>
Create the three TLS-mode CI values files that CI will use to lint and template the chart in all three TLS configurations (letsencrypt, selfsigned, custom).

Purpose: CI-04 requires that chart/clay/ci/ contains three TLS values files for lint/template validation. These files follow the self-contained pattern established by managed-values.yaml and external-values.yaml.

Output: Three new YAML files in chart/clay/ci/ that each pass helm lint and helm template independently.
</objective>

<execution_context>
@/shared/pottery-shop/.claude/get-shit-done/workflows/execute-plan.md
@/shared/pottery-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-validation-extension/05-CONTEXT.md
@.planning/phases/05-ci-validation-extension/05-RESEARCH.md

<interfaces>
<!-- Executor must replicate the structure of the existing CI values files exactly. -->

From chart/clay/ci/managed-values.yaml (pattern to replicate):
```yaml
# CI test values for managed CNPG mode
secrets:
  ADMIN_PASS: "ci-test-only"
  SESSION_SECRET: "ci-test-session-secret-not-for-production"

postgres:
  managed: true
  cluster:
    instances: 1
    storage:
      size: 1Gi
      storageClass: ""
  external:
    dsn: ""
```

From chart/tests/helm-template-test.sh (secretName must match):
```bash
CUSTOM_INGRESS=(
  --set ingress.enabled=true
  --set ingress.host=shop.example.com
  --set ingress.tls.mode=custom
  --set ingress.tls.secretName=my-tls
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three TLS CI values files</name>
  <files>
    chart/clay/ci/tls-letsencrypt-values.yaml
    chart/clay/ci/tls-selfsigned-values.yaml
    chart/clay/ci/tls-custom-values.yaml
  </files>

  <read_first>
    chart/clay/ci/managed-values.yaml
    chart/clay/ci/external-values.yaml
    chart/clay/values.yaml
    chart/tests/helm-template-test.sh
  </read_first>

  <action>
Create three new YAML files in chart/clay/ci/. Each file is self-contained (per D-03) and includes the managed Postgres baseline (per D-01) and CI placeholder secrets (per D-02). The exact content for each file is specified below. Do NOT deviate from these values.

**File 1: chart/clay/ci/tls-letsencrypt-values.yaml** (per D-04)
```yaml
# CI test values for letsencrypt TLS mode
secrets:
  ADMIN_PASS: "ci-test-only"
  SESSION_SECRET: "ci-test-session-secret-not-for-production"

postgres:
  managed: true
  cluster:
    instances: 1
    storage:
      size: 1Gi
      storageClass: ""
  external:
    dsn: ""

ingress:
  enabled: true
  host: shop.example.com
  tls:
    mode: letsencrypt
    acme:
      email: admin@example.com
```

**File 2: chart/clay/ci/tls-selfsigned-values.yaml** (per D-05)
```yaml
# CI test values for selfsigned TLS mode
secrets:
  ADMIN_PASS: "ci-test-only"
  SESSION_SECRET: "ci-test-session-secret-not-for-production"

postgres:
  managed: true
  cluster:
    instances: 1
    storage:
      size: 1Gi
      storageClass: ""
  external:
    dsn: ""

ingress:
  enabled: true
  host: shop.example.com
  tls:
    mode: selfsigned
```

**File 3: chart/clay/ci/tls-custom-values.yaml** (per D-06)
```yaml
# CI test values for custom TLS mode
secrets:
  ADMIN_PASS: "ci-test-only"
  SESSION_SECRET: "ci-test-session-secret-not-for-production"

postgres:
  managed: true
  cluster:
    instances: 1
    storage:
      size: 1Gi
      storageClass: ""
  external:
    dsn: ""

ingress:
  enabled: true
  host: shop.example.com
  tls:
    mode: custom
    secretName: my-tls
```

Note: The custom file uses `secretName: my-tls` which matches the `CUSTOM_INGRESS` variable in `chart/tests/helm-template-test.sh` (per D-06).
  </action>

  <verify>
    <automated>helm lint chart/clay/ --values chart/clay/ci/tls-letsencrypt-values.yaml && helm lint chart/clay/ --values chart/clay/ci/tls-selfsigned-values.yaml && helm lint chart/clay/ --values chart/clay/ci/tls-custom-values.yaml && helm template clay chart/clay/ --values chart/clay/ci/tls-letsencrypt-values.yaml > /dev/null && helm template clay chart/clay/ --values chart/clay/ci/tls-selfsigned-values.yaml > /dev/null && helm template clay chart/clay/ --values chart/clay/ci/tls-custom-values.yaml > /dev/null && echo "ALL PASS"</automated>
  </verify>

  <acceptance_criteria>
- `chart/clay/ci/tls-letsencrypt-values.yaml` exists and contains `mode: letsencrypt`
- `chart/clay/ci/tls-letsencrypt-values.yaml` contains `email: admin@example.com`
- `chart/clay/ci/tls-letsencrypt-values.yaml` contains `host: shop.example.com`
- `chart/clay/ci/tls-selfsigned-values.yaml` exists and contains `mode: selfsigned`
- `chart/clay/ci/tls-selfsigned-values.yaml` does NOT contain `acme:` or `secretName:`
- `chart/clay/ci/tls-custom-values.yaml` exists and contains `mode: custom`
- `chart/clay/ci/tls-custom-values.yaml` contains `secretName: my-tls`
- All three files contain `ADMIN_PASS: "ci-test-only"`
- All three files contain `SESSION_SECRET: "ci-test-session-secret-not-for-production"`
- All three files contain `managed: true`
- `helm lint` exits 0 for all three files
- `helm template` exits 0 for all three files
  </acceptance_criteria>

  <done>
Three self-contained TLS CI values files exist in chart/clay/ci/, each passes helm lint and helm template independently, letsencrypt file has acme.email, custom file has secretName: my-tls matching helm-template-test.sh, selfsigned file has no extra keys.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI values files -> Helm chart | Values override chart defaults during lint/template; no cluster access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | CI values files committed with placeholder secrets | accept | Secrets are intentional CI placeholders (`ci-test-only`), matching established pattern in managed-values.yaml and external-values.yaml. They are never used in any real deployment. Per D-02. |
| T-05-02 | Tampering | CI values files could be modified to skip validation | accept | Files are checked into version control; any modification is visible in PR diff. Behavioral test script (helm-template-test.sh) independently validates chart behavior regardless of CI values file content. |
</threat_model>

<verification>
1. `ls chart/clay/ci/tls-*.yaml` returns exactly three files
2. `helm lint chart/clay/ --values chart/clay/ci/tls-letsencrypt-values.yaml` exits 0
3. `helm lint chart/clay/ --values chart/clay/ci/tls-selfsigned-values.yaml` exits 0
4. `helm lint chart/clay/ --values chart/clay/ci/tls-custom-values.yaml` exits 0
5. `helm template clay chart/clay/ --values chart/clay/ci/tls-letsencrypt-values.yaml` renders Ingress + ClusterIssuer + Certificate
6. `helm template clay chart/clay/ --values chart/clay/ci/tls-selfsigned-values.yaml` renders Ingress + 2 ClusterIssuers + 2 Certificates
7. `helm template clay chart/clay/ --values chart/clay/ci/tls-custom-values.yaml` renders Ingress with secretName: my-tls, zero cert-manager resources
</verification>

<success_criteria>
CI-04 is fully satisfied: chart/clay/ci/ contains tls-letsencrypt-values.yaml, tls-selfsigned-values.yaml, and tls-custom-values.yaml, each passing helm lint and helm template independently.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-validation-extension/05-01-SUMMARY.md`
</output>
