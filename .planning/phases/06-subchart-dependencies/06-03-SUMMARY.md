---
phase: 06-subchart-dependencies
plan: "03"
subsystem: infra
tags: [helm, cloudnative-pg, cert-manager, subchart, kubernetes, chart-lock, dependency-update]

# Dependency graph
requires:
  - phase: 06-01
    provides: Chart.yaml dependencies block with cloudnative-pg@0.28.0 and cert-manager@v1.20.2
  - phase: 06-02
    provides: values.schema.json boolean enforcement and clay.validateDB render-time guard
provides:
  - chart/clay/Chart.lock with SHA256 digest for both operator subcharts
  - chart/clay/charts/ tarballs downloaded from official Helm repos (gitignored)
  - Full helm lint verification across default, pre-installed, and mixed toggle combinations
  - Schema enforcement confirmed (non-boolean toggle value rejected by helm lint)
  - validateDB guard confirmed (fires when postgres.managed=false with no DSN)
affects:
  - Phase 7 onward — chart is now installable with bundled operators (Chart.lock + tarballs present)
  - CI/CD workflows — helm dependency update step needed before lint in pipelines

# Tech tracking
tech-stack:
  added:
    - cloudnative-pg Helm repo (https://cloudnative-pg.github.io/charts)
    - jetstack Helm repo (https://charts.jetstack.io)
  patterns:
    - Chart.lock committed to git for digest-pinned reproducibility (T-06-10)
    - charts/ tarballs gitignored — fetched at install time, locked by Chart.lock digest
    - helm repo add + helm dependency update sequence as pre-lint prerequisite

key-files:
  created:
    - chart/clay/Chart.lock
  modified: []

key-decisions:
  - "Chart.lock committed to git (not gitignored) for audit trail of approved operator versions — tarballs in charts/ are gitignored but Chart.lock must not be (T-06-10)"
  - "charts/ tarballs gitignored per existing .gitignore — reproducibility achieved via Chart.lock digest, not tarball storage"

patterns-established:
  - "Helm dependency lifecycle: helm repo add → helm repo update → helm dependency update → commit Chart.lock"
  - "lint smoke test uses --set secrets.ADMIN_PASS=x --set secrets.SESSION_SECRET=x as minimal required value overrides"

requirements-completed:
  - CHART-04

# Metrics
duration: 15min
completed: 2026-04-15
---

# Phase 06 Plan 03: Dependency Update and Lint Verification Summary

**helm dependency update produced Chart.lock with SHA256 digest for cloudnative-pg@0.28.0 and cert-manager@v1.20.2; full lint verification confirmed toggle enforcement and validateDB guard across all combinations**

## Performance

- **Duration:** 15 min
- **Started:** 2026-04-15T02:05:00Z
- **Completed:** 2026-04-15T02:20:32Z
- **Tasks:** 2 (+ human-verify checkpoint approved)
- **Files modified:** 1 (Chart.lock created)

## Accomplishments

- Added cloudnative-pg and jetstack Helm repositories, ran `helm dependency update chart/clay` — exits 0
- chart/clay/Chart.lock generated with digest `sha256:9bf0ddd4ea12797e101c30efc56db09aebac2dd00eecc0cdf279dea13c4c47df`
- chart/clay/charts/ populated with both operator tarballs: `cloudnative-pg-0.28.0.tgz` (188K) and `cert-manager-v1.20.2.tgz` (144K)
- `helm lint chart/clay` (default, both enabled) exits 0 — 1 chart linted, 0 failed
- `helm lint` with both operators disabled also exits 0 — pre-installed mode confirmed
- Schema enforcement verified: `cloudnative-pg.enabled=notabool` rejected with schema error
- validateDB guard verified: `postgres.managed=false` with no DSN produces `postgres.managed or postgres.external.dsn required`
- validateDB silent when `postgres.managed=true` or `postgres.external.dsn` is set
- Human verified Chart.lock content and lint results at checkpoint — approved

## Task Commits

Each task was committed atomically:

1. **Task 1: Run helm dependency update and verify Chart.lock and tarballs** - `2311d82` (chore)
2. **Task 2: Full lint verification across toggle combinations** - included in `2311d82` (verification ran, no files changed)

**Worktree merge:** `(merge commit)` (chore: merge executor worktree (06-03))

## Files Created/Modified

- `chart/clay/Chart.lock` - Generated by `helm dependency update`; contains SHA256 digest pinning both operator subcharts. Committed to git per T-06-10.
- `chart/clay/charts/cloudnative-pg-0.28.0.tgz` - Downloaded CNPG operator chart (188K); gitignored, fetched at install time
- `chart/clay/charts/cert-manager-v1.20.2.tgz` - Downloaded cert-manager chart (144K); gitignored, fetched at install time

## Decisions Made

- Chart.lock committed to git (not gitignored): provides audit trail of which operator versions were tested and approved, enabling reproducible installs. Satisfies T-06-10 mitigate disposition.
- The charts/ tarballs themselves are gitignored per the existing `.gitignore` — Chart.lock digest provides integrity verification without storing large binaries in git.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - `helm dependency update` fetched both charts on first attempt. All lint tests produced expected results without any debugging required.

## User Setup Required

None - no external service configuration required. Chart repos are public; no authentication needed.

## Next Phase Readiness

Phase 6 complete. All three plans delivered:
- 06-01: Chart.yaml dependencies block with operator toggle conditions
- 06-02: values.schema.json boolean enforcement and clay.validateDB guard
- 06-03: Chart.lock + tarballs + full lint verification

The chart is now installable with bundled CNPG and cert-manager operators. Phase 7 (Cluster CR and database wiring) can proceed — `postgres.managed=true` renders cleanly, and `helm lint` is green in all tested toggle combinations.

## Known Stubs

None - no placeholder data or TODO stubs introduced.

## Threat Flags

None - no new network endpoints, auth paths, or trust boundaries introduced. Chart.lock commit satisfies T-06-10 mitigate disposition established in the plan's threat model.

## Self-Check: PASSED

- chart/clay/Chart.lock: FOUND (committed in 2311d82)
- .planning/phases/06-subchart-dependencies/06-03-SUMMARY.md: this file
- Commit 2311d82 (Task 1 — dependency update): FOUND in git log
- merge commit for worktree-agent-a1a18c39: FOUND in git log

---
*Phase: 06-subchart-dependencies*
*Completed: 2026-04-15*
