---
phase: "14-firing-logs-crud"
plan: 2
type: execute
wave: 2
depends_on:
  - 14-01-PLAN.md
  - 11-02-PLAN.md
files_modified:
  - internal/handlers/firing_log.go
  - cmd/server/main.go
autonomous: true
requirements:
  - SELL-10

must_haves:
  truths:
    - "All /dashboard/firings/* routes require a valid seller session (requireSeller guard)"
    - "GET /dashboard/firings lists only the logged-in seller's firing logs"
    - "POST /dashboard/firings/create creates a log and redirects to its detail page"
    - "GET /dashboard/firings/{id} returns 403 if session.SellerID != log.SellerID"
    - "POST /dashboard/firings/{id}/update saves readings batch via FiringLogStore.SaveReadings"
    - "POST /dashboard/firings/{id}/delete removes the log (CASCADE clears readings)"
  artifacts:
    - path: "internal/handlers/firing_log.go"
      provides: "FiringLogHandler with all CRUD methods"
      contains: "type FiringLogHandler struct"
  key_links:
    - from: "internal/handlers/firing_log.go FiringLogHandler.View()"
      to: "internal/models/firing_log.go FiringLogStore.GetByID()"
      via: "ownership enforced at model layer by passing sellerID"
      pattern: "log, err := h.logs.GetByID(ctx, id, session.SellerID)"
    - from: "internal/handlers/firing_log.go FiringLogHandler.Update()"
      to: "internal/models/firing_log.go FiringLogStore.SaveReadings()"
      via: "parse readings from form POST body, pass slice to store"
      pattern: "err = h.logs.SaveReadings(ctx, id, session.SellerID, readings)"
---

<objective>
Implement the `FiringLogHandler` with all seven CRUD routes for firing logs. Ownership is enforced by passing `session.SellerID` to model methods — the store rejects requests for logs belonging to other sellers. No template work in this plan — templates come in Plan 3.

The handler parses multi-row readings from the form POST (a table of rows submitted as repeated field names with indices), builds a `[]FiringReading` slice, and passes it to `SaveReadings`.
</objective>

<execution_context>
@/shared/pottery-shop/.claude/get-shit-done/workflows/execute-plan.md
@/shared/pottery-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-firing-logs-crud/14-CONTEXT.md
</context>

<task_breakdown>
## Tasks

### Task 1: FiringLogHandler — all CRUD methods
Create `internal/handlers/firing_log.go`:

```go
type FiringLogHandler struct {
    logs      *models.FiringLogStore
    sessions  *middleware.SessionManager
    templates *template.Template
}
```

Methods (all require `requireSeller` guard applied at route registration):

- `List(w, r)` — GET /dashboard/firings; calls `ListBySeller(ctx, session.SellerID)`; renders firings_list.html
- `New(w, r)` — GET /dashboard/firings/new; renders firings_form.html with empty FiringLog
- `Create(w, r)` — POST /dashboard/firings/create; parses title/clay_body/glaze_notes/outcome/notes/firing_date; calls `FiringLogStore.Create`; redirects to `/dashboard/firings/{id}`
- `View(w, r)` — GET /dashboard/firings/{id}; calls `GetByID(ctx, id, session.SellerID)`; if nil → 403; renders firings_detail.html
- `Edit(w, r)` — GET /dashboard/firings/{id}/edit; calls `GetByID`; if nil → 403; renders firings_form.html with existing data and readings
- `Update(w, r)` — POST /dashboard/firings/{id}/update; parses log fields + readings table from form; calls `Update` then `SaveReadings`; redirects to `/dashboard/firings/{id}`
- `Delete(w, r)` — POST /dashboard/firings/{id}/delete; calls `Delete(ctx, id, session.SellerID)`; redirects to `/dashboard/firings`

Form parsing for readings: the form submits rows as `readings[0][elapsed_minutes]`, `readings[0][temperature]`, etc. Parse by iterating i=0,1,2... until the key is missing. Skip rows where elapsed_minutes or temperature is empty.

Files:
- `internal/handlers/firing_log.go` — new file

### Task 2: Register routes in main.go
Read `cmd/server/main.go`.

Instantiate `FiringLogHandler` with `FiringLogStore`, `SessionManager`, templates.

Register routes, all wrapped with `requireSeller`:
- `GET /dashboard/firings` → `firingLogHandler.List`
- `GET /dashboard/firings/new` → `firingLogHandler.New`
- `POST /dashboard/firings/create` → `firingLogHandler.Create`
- `GET /dashboard/firings/{id}` → `firingLogHandler.View`
- `GET /dashboard/firings/{id}/edit` → `firingLogHandler.Edit`
- `POST /dashboard/firings/{id}/update` → `firingLogHandler.Update`
- `POST /dashboard/firings/{id}/delete` → `firingLogHandler.Delete`

Files:
- `cmd/server/main.go` — add FiringLogHandler init and route registration
</task_breakdown>
