---
phase: "16-admin-seller-oversight"
plan: 2
type: execute
wave: 2
depends_on:
  - 16-01-PLAN.md
  - 12-01-PLAN.md
files_modified:
  - internal/handlers/admin.go
  - templates/admin/dashboard.html
autonomous: true
requirements:
  - SELL-16

must_haves:
  truths:
    - "Admin dashboard product list shows a Seller column with each product's seller name"
    - "Admin dashboard uses ListAllWithSeller to populate SellerName on each Product"
    - "Clicking a seller name in the admin products table links to /admin/sellers (or /seller/{id} profile)"
    - "No firing log routes or data appear anywhere under /admin"
  artifacts:
    - path: "templates/admin/dashboard.html"
      provides: "Admin products table with Seller column"
      contains: "SellerName"
  key_links:
    - from: "internal/handlers/admin.go AdminHandler.Dashboard()"
      to: "internal/models/product.go ProductStore.ListAllWithSeller()"
      via: "admin fetches all products with JOIN to sellers for SellerName"
      pattern: "products, err := h.products.ListAllWithSeller(ctx)"
---

<objective>
Update the admin dashboard products view to show a Seller column so the admin can see at a glance which seller owns each product. This is a targeted change to the existing `AdminHandler.Dashboard()` method (already updated in Phase 12 Plan 2) and its template.

This plan confirms the Phase 12 changes are complete and adds any remaining template polish needed for the Seller column, then verifies that no firing log data appears anywhere in the admin area.
</objective>

<execution_context>
@/shared/pottery-shop/.claude/get-shit-done/workflows/execute-plan.md
@/shared/pottery-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-admin-seller-oversight/16-CONTEXT.md
</context>

<task_breakdown>
## Tasks

### Task 1: Verify and complete admin products Seller column
Read `internal/handlers/admin.go` and `templates/admin/dashboard.html`.

Verify that `AdminHandler.Dashboard()` calls `ProductStore.ListAllWithSeller(ctx)` (was wired in Phase 12 Plan 2). If not yet done, update it now.

Verify that `templates/admin/dashboard.html` renders a "Seller" column in the products table. If not yet done, add:
```html
<th>Seller</th>
```
and in each row:
```html
<td>
  {{if .SellerName}}
  <a href="/seller/{{.SellerID}}">{{.SellerName}}</a>
  {{else}}—{{end}}
</td>
```

Add a "Sellers" link in the admin navigation area (top of page or sidebar) pointing to `/admin/sellers`.

Files:
- `internal/handlers/admin.go` — confirm ListAllWithSeller is used in Dashboard()
- `templates/admin/dashboard.html` — add Seller column and Sellers nav link

### Task 2: Confirm no firing log routes in admin
Read `cmd/server/main.go` and review all routes registered under `/admin/*`.

Confirm that:
- No route under `/admin` calls any `FiringLogStore` method
- No route under `/admin` renders firing log data

If any accidental firing log route was added under `/admin`, remove it. Firing log access is strictly under `/dashboard/firings/*` and `/api/firings/*`, owner-only.

This task is a review + potential fix, not a guaranteed file edit. Document findings in the plan summary.

Files:
- `cmd/server/main.go` — review only; remove any `/admin/firings` routes if found
</task_breakdown>
