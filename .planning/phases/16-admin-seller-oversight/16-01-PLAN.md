---
phase: "16-admin-seller-oversight"
plan: 1
type: execute
wave: 1
depends_on:
  - 11-01-PLAN.md
  - 11-02-PLAN.md
files_modified:
  - internal/handlers/admin.go
  - cmd/server/main.go
  - templates/admin/sellers.html
autonomous: true
requirements:
  - SELL-14
  - SELL-15

must_haves:
  truths:
    - "GET /admin/sellers lists all sellers with name, email, is_active status, and approve/reject buttons"
    - "POST /admin/sellers/approve sets is_active=true for the given seller ID"
    - "POST /admin/sellers/reject deletes or sets is_active=false for the given seller ID (soft-delete preferred)"
    - "Admin seller routes are guarded by existing Basic Auth middleware"
    - "No route under /admin exposes firing log data"
  artifacts:
    - path: "internal/handlers/admin.go"
      provides: "SellerList, ApproveSeller, RejectSeller methods on AdminHandler"
      contains: "SellerList"
    - path: "templates/admin/sellers.html"
      provides: "Admin seller management page"
      contains: "approve"
  key_links:
    - from: "internal/handlers/admin.go AdminHandler.SellerList()"
      to: "internal/models/seller.go SellerStore.ListAll()"
      via: "admin handler fetches all sellers for display"
      pattern: "sellers, err := h.sellers.ListAll(ctx)"
    - from: "internal/handlers/admin.go AdminHandler.ApproveSeller()"
      to: "internal/models/seller.go SellerStore.SetActive()"
      via: "handler calls SetActive(ctx, id, true)"
      pattern: "h.sellers.SetActive(ctx, id, true)"
    - from: "internal/handlers/admin.go AdminHandler.RejectSeller()"
      to: "internal/models/seller.go SellerStore.SetActive()"
      via: "handler calls SetActive(ctx, id, false) for soft-reject"
      pattern: "h.sellers.SetActive(ctx, id, false)"
---

<objective>
Add seller management to the admin area: a list page shows all sellers with their status, and approve/reject buttons let the admin activate or deactivate accounts. These routes are protected by the existing Basic Auth middleware. No firing log data is accessible from any admin route.

The `ApproveSeller` route supplements (does not replace) the email-token approval added in Phase 11 Plan 3.
</objective>

<execution_context>
@/shared/pottery-shop/.claude/get-shit-done/workflows/execute-plan.md
@/shared/pottery-shop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-admin-seller-oversight/16-CONTEXT.md
</context>

<task_breakdown>
## Tasks

### Task 1: Admin seller handler methods
Read `internal/handlers/admin.go` to understand the existing `AdminHandler` struct and patterns.

Add to `AdminHandler`:
- `SellerList(w, r)` — GET /admin/sellers; calls `SellerStore.ListAll(ctx)`; renders `admin/sellers.html` with `[]Seller`
- `ApproveSeller(w, r)` — POST /admin/sellers/approve; reads `seller_id` from form; calls `SellerStore.SetActive(ctx, id, true)`; redirects to `/admin/sellers`
- `RejectSeller(w, r)` — POST /admin/sellers/reject; reads `seller_id` from form; calls `SellerStore.SetActive(ctx, id, false)`; redirects to `/admin/sellers`

Add `sellers *models.SellerStore` field to `AdminHandler` struct. Inject in `main.go` when constructing the handler.

Files:
- `internal/handlers/admin.go` — add SellerStore field and three methods

### Task 2: Register admin seller routes
Read `cmd/server/main.go`.

Add after existing admin routes (all guarded by the existing Basic Auth middleware):
- `GET /admin/sellers` → `adminHandler.SellerList`
- `POST /admin/sellers/approve` → `adminHandler.ApproveSeller`
- `POST /admin/sellers/reject` → `adminHandler.RejectSeller`

Also add a "Sellers" navigation link in the admin dashboard template or sidebar.

Files:
- `cmd/server/main.go` — register three admin seller routes

### Task 3: Admin sellers template
Create `templates/admin/sellers.html`:

- Page title: "Sellers"
- Table columns: Name | Email | Status | Registered | Actions
- Status column: "Active" (green) or "Pending Approval" (orange) based on `IsActive`
- Actions column:
  - If pending: "Approve" button (POST /admin/sellers/approve, seller_id hidden input) + "Reject" button (POST /admin/sellers/reject)
  - If active: "Deactivate" button (POST /admin/sellers/reject) — same endpoint, soft-disable
- Empty state: "No sellers registered yet."
- Back link to /admin

Files:
- `templates/admin/sellers.html` — new
</task_breakdown>
