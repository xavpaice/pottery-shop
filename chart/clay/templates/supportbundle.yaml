apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clay.fullname" . }}-support-bundle
  labels:
    {{- include "clay.labels" . | nindent 4 }}
    troubleshoot.sh/kind: support-bundle
type: Opaque
stringData:
  support-bundle.yaml: |
{{ include "clay.supportbundle" . | indent 4 }}
